import { useState } from "react";
import { advertisementService } from "../services/advertisementService";
export default function CreateAdvertisementPage(){const [form,setForm]=useState({title:"",description:"",type:"Lost",catName:"",breed:"",furColor:"",city:"",area:"",contactPhoneNumber:"",contactEmail:""});function updateField(e){const {name,value}=e.target;setForm((current)=>({...current,[name]:value}))}async function handleSubmit(e){e.preventDefault();await advertisementService.create(form);alert("Annons skapad!")}return <section className="page"><h1>Skapa annons</h1><form className="form" onSubmit={handleSubmit}><input className="input" name="title" placeholder="Titel" value={form.title} onChange={updateField}/><textarea className="input" name="description" placeholder="Beskrivning" value={form.description} onChange={updateField}/><select className="input" name="type" value={form.type} onChange={updateField}><option value="Lost">Försvunnen katt</option><option value="Found">Upphittad katt</option></select><input className="input" name="catName" placeholder="Kattens namn" value={form.catName} onChange={updateField}/><input className="input" name="breed" placeholder="Ras" value={form.breed} onChange={updateField}/><input className="input" name="furColor" placeholder="Pälsfärg" value={form.furColor} onChange={updateField}/><input className="input" name="city" placeholder="Stad" value={form.city} onChange={updateField}/><input className="input" name="area" placeholder="Område" value={form.area} onChange={updateField}/><input className="input" name="contactPhoneNumber" placeholder="Telefonnummer" value={form.contactPhoneNumber} onChange={updateField}/><input className="input" name="contactEmail" placeholder="Email" value={form.contactEmail} onChange={updateField}/><button className="btn">Skapa annons</button></form></section>}
